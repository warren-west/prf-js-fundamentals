// IPO = Input, Processing, Output
function getBurp(num) {
    let first = "Bu"
    let second = ""
    let third = "p"
    
    for (let i = 0; i < num; i++) {
        // Special formula / logic for
        // concatenating or summing up
        second += "r" // this is the same as second = second + "r"
    
        // console.log(i, second)
    }
    
    
    alert(first + second + third)
}


// invoke the function

let userInput = prompt("Enter num:")
getBurp(userInput)




// get a number from the user

// set variables for BU and p

// loop from 1 to num / x / 5

// add "r" to your middle piece

// finish the loop

// return the result -> BU + the "r"s + p

// My output is Burp every time :(